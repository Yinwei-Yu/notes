Time:2025-10-19

Status: 

- [ ] **working** 👨‍💻
- [x] *done*    💻

Tags:[[3-tags/cg|cg]]

## What have we covered so far?

物体和相机位置变化
物体相对相机坐标
投影到屏幕
三角形采样

next?->着色

## Shading

阴影,颜色的不同

Blinn-Phong反射模型

三种区域:高光,漫反射,环境光

定义参数:
观察方向:v
面法向量:n
光方向:l
(三个都是单位向量)
表面参数:颜色,亮度...

(shading is local,不考虑阴影)

## diffuse reflection

漫反射
收到多少能量?

![[2-source-material/images/Pasted image 20251019140007.png]]

法线和光线的夹角->接收能量多少
光强:$I = \frac{I_0}{r^2}$

$L_d$:diffusely reflected light
$k_d$:diffuse coefficient(color)

$$ L _ {d} = k _ {d} \left(I / r ^ {2}\right) \max (0, \mathbf {n} \cdot \mathbf {l}) $$

max(0,n\*l)?->点乘为0,不考虑物理意义

漫反射的光均匀分布->与观察方向无关

## Specular Term

光滑物体表面
反射接近镜面反射(lnR)->v和R接近

v和R接近->半程向量h和n接近

![[2-source-material/images/Pasted image 20251019142104.png]]

为什么没考虑能量吸收n\*l?->Blinn-Phong简化了
为什么用n和h夹角不用v和R?->计算简单
指数$p$是啥?->$cos \alpha$的下降率太低,加指数增大下降率,通常100-200->控制高光大小

## Ambient Term(环境光)

假设环境光强都相同:

$L_a = K_a*l_a$

相当于一个常数颜色,无论在哪儿都一样

## Blinn-Phong Reflection Model

![[2-source-material/images/Pasted image 20251019142735.png]]

## Shading Frequencies(着色频率)

着色频率->着色需要应用到哪些点上
一个面->顶点->像素

Flat Shading

• Triangle face is flat — one normal vector
• Not good for smooth surfaces

---

Gouraud shading
• Interpolate colors from vertices across triangle
• Each vertex has a normal vector (how?)

每个顶点求法线,求着色,内部颜色差值得出

---

Phong shading
• Interpolate normal vectors across each triangle
• Compute full shading model at each pixel
• Not the Blinn-Phong Reflectance Model(着色模型和着色频率不一样!)

对每个像素着色

---

顶点法向量:

一个顶点和多个三角形关联->顶点法向量用三角形的面法向量平均
优化:三角形面加权平均

---

像素法向量:

用重心坐标

## 图形管线(Graphics Pipeline)

空间点->投影到屏幕上->形成三角形->光栅化->着色->输出

着色可能发生在不同阶段->顶点或者像素

## Shader

```c 
uniform sampler2D myTexture; 
uniform vec3 lightDir; 
varying vec2 uv; 
varying vec3 norm; 
void diffuseShader() { 
vec3 kd; 
kd = texture2d(myTexture, uv); 
kd  *=clamp(DOT(-lightDir,norm),0.0,1.0); 
gl_FragColor $=$ vec4(kd,1.0); 
```

GPU
 
着色器可编程
compute shader

## Texture Mapping

点的渲染不同,只是kd的不同->希望定义物体表面不同点的属性

任何3d物体的表面->二维

纹理就是一张图->蒙在物体表面(映射)

三角形映射(美工,自动化的活儿),假设我们已知

三角形顶点被映射到纹理坐标系(u,v),范围在$[0,1]^2$内

一个纹理被映射多次->边缘处相接->纹理无缝衔接


## Barycentric coordinates（重心坐标）


为什么插值？
  希望三角形内部的值和顶点相关
  平滑过渡

通过插值要什么？
  纹理,颜色,法向量...

如何插值?
  重心坐标

what?
  重心坐标是定义在一个三角形所在平面内的
  任意(x,y):
  (x, y) = αA+βB+γC
  α+β+γ=1
  如果三个系数都非负,则该点在三角形内部

example:A->(α,β,γ)=(1,0,0)

面积比重心坐标:
  ![[2-source-material/images/Pasted image 20251022221142.png]]

三角形重心:(x,y) = 1/3A+1/3B+1/3C

任意一个点的重心坐标可由公式求出:见slide

so,属性线性组合->属性插值

**投影会导致重心坐标改变**

so->使用三维空间的坐标来插值,不能用投影后的坐标插值

## 纹理应用

![[2-source-material/images/Pasted image 20251022221954.png]]

---

Texture Magnification(纹理放大问题)

*纹理太小*->(u,v)不在整数处->round
(a pixel on a texture--a texel)->多个pixel->同个texel
三种方法:nearest,bilinear,bicubic

双线性插值:

找到一个texel邻近四个点:
texel距左下角点的水平垂直距离:s,t
线性插值:lerp (x,v0,v1)=v0 + x(v1-v0)

![[2-source-material/images/Pasted image 20251022222611.png]]

*纹理太大*--aliasing

why?
  远处一个像素覆盖了很大的一个纹理区域--一个texel不能代表一个区域--也就是采样率太低

how?
  超采样->每个像素采样512个点--太慢

不采样可以吗?->能不能直接搞到区域的平均值?

## Mipmap

允许范围查询:快,大约,支持方形范围查询

把纹理每次缩小到原来的一半->提前生成,然后存储

![[2-source-material/images/Pasted image 20251022224024.png]]

"图像金字塔"

额外存储量?->等比数列求和--多了1/3

咋知道像素对应某个区域的大小?->
![[2-source-material/images/Pasted image 20251022224754.png]]

问题:每一层之间太跳脱->不连续
插值!->三线型插值--level D插值 level D+1插值--两个结果再插值

*还有一些问题!*

会导致远处细节丢失,过模糊!

---

采用各向异性过滤解决该问题

mipmap只把长宽同时减小一半,也就是只做了对角线:
采用ripmap

![[2-source-material/images/Pasted image 20251022225831.png]]

对矩形区域友好,但是不好做斜区域的查询

EWA filtering
  多次查询
  加权平均

## 纹理的应用

现代GPU：纹理=内存+查询

*纹理可以用来描述环境光->渲染其他物体*

球环境贴图->展开->扭曲问题
解决:沿球心把点映射到cube上,然后展开

本质上要反应光照信息

*纹理可以用来描述凹凸贴图*

定义基础表面沿法线上下移动多少->凹凸

扰动法线->纹理定义了点相对高度的移动->点的位置在凹凸贴图上的位置改变->法线方向改变

如何计算?

![](2-source-material/images/Pasted%20image%2020251027101801.png)

in 3d?

![](2-source-material/images/Pasted%20image%2020251027101924.png)

**注:** 这些是在局部坐标系中做的,然后把法线重新计算回世界坐标中

更现代的做法:**位移贴图**

使用纹理贴图相同的输入,然后做真正的移动顶点位置,而不只是改变法线位置

*3d噪声+实体建模*

定义一个三维的噪声函数->每个点都有对应值

对值进行一些操作后,得到想要的纹理效果

*环境光遮蔽*

把环境光结果放到texture map中,然后贴上就行->计算提前并记录在纹理中

*三维纹理和体积渲染*

## shadows

shadow mapping 处理点光源 硬阴影

光栅化方法

图像空间算法,不需要知道几何信息
问题:走样

点不在阴影内,则相机和光源都要看到这个点

1. 从光源看向场景,记录深度
2. 从eye看向场景,把看到的点投影到光源,看在哪个位置,比较eye看到的到光源的深度和之前记录的

![](2-source-material/images/Pasted%20image%2020251031152851.png)

问题:

判断相等需要浮点判断,很困难->导致画面有些部分"脏"

shadow map的分辨率->走样问题

---

hard vs soft

硬阴影锐利,软阴影相对柔化

本影 半影

软阴影->光源大小

## Reference

[[2-source-material/slides/GAMES101_Lecture_07.pdf|GAMES101_Lecture_07]]

[[2-source-material/slides/GAMES101_Lecture_08.pdf|GAMES101_Lecture_08]]

[[2-source-material/slides/GAMES101_Lecture_09.pdf|GAMES101_Lecture_09]]